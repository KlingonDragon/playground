{
  "name": "ts-app",
  "version": "1.0.0",
  "description": "Debug and Testing",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=app --format=esm && cp src/*.html src/*.css app/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}